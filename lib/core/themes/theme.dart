import 'package:flutter/material.dart';

class AppTheme {
  // Kolor główny aplikacji
  static const Color primaryColor = Color(0xFF6200EE);

  // Kolor akcentu
  static const Color accentColor = Color(0xFF03DAC5);

  // Kolor tła
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Kolor tekstu na tle jasnym
  static const Color lightTextColor = Colors.black;

  // Kolor tekstu na tle ciemnym
  static const Color darkTextColor = Colors.white;

  // Kolor cienia
  static const Color shadowColor = Colors.grey;

  // Kolor przycisku
  static const Color buttonColor = Color(0xFF6200EE);

  // Kolor tekstu na przycisku
  static const Color buttonText = Colors.white;

  // Kolor podświetlenia przycisku
  static const Color buttonHighlightColor = Color(0xFF3700B3);

  // Kolor linii oddzielającej
  static const Color dividerColor = Colors.grey;

  // Kolor nagłówka
  static const Color headerColor = Color(0xFF6200EE);

  // Kolor podświetlenia
  static const Color highlightColor = Color(0xFF03DAC5);
}
